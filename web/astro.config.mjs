import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // site: 'https://vcap.example.com', // set when a real domain is assigned
  compressHTML: true,
});
